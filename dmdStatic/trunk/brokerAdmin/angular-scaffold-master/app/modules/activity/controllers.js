activityModule.controller('actlistController', ['$scope', '$state', 'activityService',
	function($scope, $state, service) {
		
	}
]);
activityModule.controller('acteditController', ['$scope', '$state', 'activityService',
	function($scope, $state, service) {
		
	}
]);
activityModule.controller('awardlistController', ['$scope', '$state', 'activityService',
	function($scope, $state, service) {
		
	}
]);
activityModule.controller('awardeditController', ['$scope', '$state', 'activityService',
	function($scope, $state, service) {
		
	}
]);
activityModule.controller('busilistController', ['$scope', '$state', 'activityService',
	function($scope, $state, service) {
		
	}
]);
activityModule.controller('busieditController', ['$scope', '$state', 'activityService',
	function($scope, $state, service) {
		
	}
]);
activityModule.controller('itemlistController', ['$scope', '$state', 'activityService',
	function($scope, $state, service) {
		
	}
]);
activityModule.controller('itemeditController', ['$scope', '$state', 'activityService',
	function($scope, $state, service) {
		
	}
]);
activityModule.controller('ticketlistController', ['$scope', '$state', 'activityService',
	function($scope, $state, service) {
		
	}
]);
activityModule.controller('ticketeditController', ['$scope', '$state', 'activityService',
	function($scope, $state, service) {
		
	}
]);